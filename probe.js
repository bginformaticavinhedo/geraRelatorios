const { Client } = require("@microsoft/microsoft-graph-client");
require("isomorphic-fetch");

async function probe() {
    const accessToken = "TOKEN_PLACEHOLDER"; // I don't have this, but I can use the existing API routes logic
}
